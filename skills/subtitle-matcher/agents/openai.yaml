interface:
  display_name: "Subtitle Matcher"
  short_description: "Find, score, validate, normalize Chinese subtitles, and render HTML reports"
  default_prompt: "Use $subtitle-matcher to find, score, validate, and normalize Chinese subtitles for a local video folder; generate the HTML report in the video root and enable VLC report playback when on Windows."
